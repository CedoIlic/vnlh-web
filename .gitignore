# Distribucija (kopije za deployment – sadržaj je već praćen u js/ php/ itd.)
0-Distribution/

# Backup i arhive (radne kopije, ne trebaju povijest)
0-Arhiva/
0-Backup_SQL/
VNLH WEB.zip

# SQL dumpovi i backupi (ali ne i sql/ mapa - ona ima skripte aplikacije)
*.bak
*.dump
*.sqlite

# Editor/IDE postavke (osobne, ne dijele se)
.vscode/
.idea/

# OS smeće
Thumbs.db
Desktop.ini
.DS_Store

# Logovi i privremene datoteke
*.log
*.tmp
*.temp
*~

# VAŽNO: Konfiguracije s lozinkama
# .cursor/ nije cijeli ignoriran: pravila u .cursor/rules/ mogu u git; MCP ostaje izvan repoa.
php/vnlh_db_connect.php
php/config.php
config.php
.cursor/mcp.json
.env
.env.local

# Node (ako koristiš npm/build tools)
node_modules/
dist/
build/
# i18n radni JSON-i + generirani seed SQL (jednokratno, ne commitati)
tmp/
sql/i18n_*_seed.sql
# jednokratne ALTER skripte za Heidi (shema je u db-schema/ za Skeemu)
sql/alter_*.sql
# jednokratni restore podataka iz bekapa (veliki, ne commitati)
sql/restore_*.sql
# jednokratni data-update (seed/ispravak) za Heidi
sql/update_*.sql
